# Screenshot gallery

Raw captures from the Azure deployment and load test on 2026-09-03, kept as
supporting evidence. The curated, cropped versions used in the main
[README](../../../README.md#live-deployment) live one directory up:

| Used in the README | |
|---|---|
| `../azure-resource-group.png` | the resource group with every component |
| `../azure-replica-autoscale.png` | Compliance replica count rising 1 → 5 |
| `../azure-replicas-running.png` | the five replicas running at peak |
| `../cloud-dashboard.png` | the fleet dashboard under load |
| `../azure-compliance-load.png` | per-replica CPU and response time under load |

Files here are named by capture time (`2026-09-03_HH-MM-SS.png`). They include
the live dashboard at several points during the run, the Azure portal views of
the resource group, the Container App revisions/replicas and scale rules, and
the Metrics blade.
